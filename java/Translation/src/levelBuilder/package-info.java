/**
 * Contains structures that allow the creation of verification game levels using
 * higher level commands, reducing the need for clients to interact with the low
 * level details of level construction.
 */

package levelBuilder;